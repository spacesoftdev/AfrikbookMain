you dont understand i am currently obtaining this data from an other  DB id = request.user.company_id.id now what even that data may be i want it and use it to insert in to my new user table User.objects.using(db)... its a multi database the connects somehow when a use registers into my company i create an account so that when they login from the man db  i can know the user request.user.company_id.id now immediately they register  an new db will be created and a migration will be made so am obtaining request.user.company_id.id from a DB that already has data